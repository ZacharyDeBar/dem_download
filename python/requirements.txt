# With numpy>=2.5, rasterio<=1.5.0's band reads (rasterio.open(...).read())
# emit a DeprecationWarning ("Setting the shape on a NumPy array...") from
# rasterio's own internals — confirmed via a bare rasterio read outside this
# codebase, so it's not a bug here. Harmless; will resolve once rasterio
# ships a numpy-2.5-compatible release. Pin numpy<2.5 if you want it silent.
numpy>=1.24
scipy>=1.10
rasterio>=1.3
shapely>=2.0
requests>=2.28
matplotlib>=3.7
python-dotenv>=1.0
